// Physical constants. All SI.

export const G = 9.81; // m/s^2
export const AIR_DENSITY = 1.225; // kg/m^3 at sea level, 15C
export const WHEEL_RADIUS = 0.33; // m, treated as constant for v1
export const RPM_TO_RAD_S = (2 * Math.PI) / 60;
export const RAD_S_TO_RPM = 60 / (2 * Math.PI);
